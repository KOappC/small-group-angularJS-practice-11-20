(function() {
    angular
        .module("app", ["ngRoute"])
        .config(function($routeProvider) {
            $routeProvider
                .when("/group", {
                    //template: component

                })
                .when("/weather", {

                })
                .when("/prime", {

                })
        })
})();